name: syslog-mcp

# Local development stack. Builds from this checkout.
services:
  syslog-mcp:
    extends:
      file: docker-compose.prod.yml
      service: syslog-mcp
    image: syslog-mcp:dev
    pull_policy: build
    build:
      context: .
      dockerfile: config/Dockerfile

volumes:
  syslog-mcp-data:

networks:
  jakenet:
    name: ${DOCKER_NETWORK:-jakenet}
    external: true
