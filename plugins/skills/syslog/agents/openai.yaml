interface:
  display_name: "Syslog"
  short_description: "Query and analyze homelab syslog data"
  default_prompt: "Use $syslog to search recent homelab logs for errors and summarize the important findings."
dependencies:
  tools:
    - type: "mcp"
      value: "syslog"
      description: "Syslog MCP server exposing the syslog action-dispatch tool"
