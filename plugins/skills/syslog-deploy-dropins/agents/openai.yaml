interface:
  display_name: "Syslog Deploy Drop-ins"
  short_description: "Deploy rsyslog forwarding drop-ins"
  default_prompt: "Use $syslog-deploy-dropins to deploy rsyslog forwarding drop-ins across fleet hosts."
