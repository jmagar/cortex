interface:
  display_name: "Syslog Troubleshoot"
  short_description: "Diagnose cortex failures"
  default_prompt: "Use $syslog-troubleshoot to diagnose why cortex is not receiving logs from a host."
dependencies:
  tools:
    - type: "mcp"
      value: "syslog"
      description: "Syslog MCP server for hosts, stats, and recent log evidence"
