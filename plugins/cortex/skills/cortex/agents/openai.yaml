interface:
  display_name: "Cortex"
  short_description: "Query and analyze homelab syslog data"
  default_prompt: "Use $cortex to search recent homelab logs for errors and summarize the important findings."
dependencies:
  tools:
    - type: "mcp"
      value: "cortex"
      description: "cortex MCP server exposing the cortex action-dispatch tool"
